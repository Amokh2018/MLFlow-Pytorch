MLFlow also provides integration with PyTorch. 
Integrating MLFlow into PyTorch code is quite simple. 
We will be exploring a simple convolutional neural network applied to the MNIST dataset once again.
